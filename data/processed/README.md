# Processed Data

The cleaned essay dataset is uploaded in this folder.

The cleaned news dataset is not uploaded because it exceeds GitHub's web upload file size limit.  
It can be generated by running the preprocessing code in `notebooks/ai_text_detection.ipynb`.

Expected generated file: news_clean_dataset.csv
